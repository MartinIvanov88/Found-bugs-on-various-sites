// Generated by Selenium IDE
using System;
using System.Collections;
using System.Collections.Generic;
using System.Linq;
using System.Threading;
using OpenQA.Selenium;
using OpenQA.Selenium.Chrome;
using OpenQA.Selenium.Firefox;
using OpenQA.Selenium.Remote;
using OpenQA.Selenium.Support.UI;
using OpenQA.Selenium.Interactions;
using NUnit.Framework;
[TestFixture]
public class MIssingbagsinaccesoryFindafterclickheadbandTest {
  private IWebDriver driver;
  public IDictionary<string, object> vars {get; private set;}
  private IJavaScriptExecutor js;
  [SetUp]
  public void SetUp() {
    driver = new ChromeDriver();
    js = (IJavaScriptExecutor)driver;
    vars = new Dictionary<string, object>();
  }
  [TearDown]
  protected void TearDown() {
    driver.Quit();
  }
  [Test]
  public void mIssingbagsinaccesoryFindafterclickheadband() {
    driver.Navigate().GoToUrl("https://www.exza.bg/");
    driver.Manage().Window.Size = new System.Drawing.Size(1552, 840);
    driver.FindElement(By.XPath("(//a[contains(text(),\'Аксесоари\')])[2]")).Click();
    {
      var elements = driver.FindElements(By.XPath("//a[contains(text(),\'Червена чанта с пери\')]"));
      Assert.True(elements.Count == 0);
    }
    driver.FindElement(By.XPath("//a[contains(text(),\'Лента за коса L6\')]")).Click();
    Assert.That(driver.FindElement(By.XPath("//a[contains(text(),\'Червена чанта с пери\')]")).Text, Is.EqualTo("ЧЕРВЕНА ЧАНТА С ПЕРИ"));
    driver.FindElement(By.XPath("//a[contains(text(),\'Червена чанта с пери\')]")).Click();
    driver.Close();
  }
}
